class Solution:
    def intersection(self, nums1: List[int], nums2: List[int]) -> List[int]:
        st=set(nums1)
        result=set()
        for i in range(len(nums2)):
            if nums2[i] in st:
                result.add(nums2[i])
        return list(result)
