class Solution {
public:
    int finalValueAfterOperations(vector<string>& operations) {
        int x=0;
        for(auto ch:operations){
            if(ch[1]=='+')
                x++;
            else
                x--;
        }
        return x;
    }
};
