class Solution {
public:
    int fib(int n) {
        int ans=0,a=0,b=1;
        if(n<=1)
            return n;
        for(int j=2;j<=n;j++){
            ans=a+b;
            a=b;
            b=ans;
        }
        return ans;
    }
};
