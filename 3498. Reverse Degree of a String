class Solution {
public:
    int reverseDegree(string s) {
        int sum=0,total_sum=0;
        int i=1;
        for(auto c:s){
            sum=i++*(26-(c-'a'));
            total_sum+=sum;
        }
        return total_sum;
    }
};
