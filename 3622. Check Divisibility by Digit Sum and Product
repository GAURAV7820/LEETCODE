class Solution:
    def checkDivisibility(self, n: int) -> bool:
        n_string=str(n)
        p=1
        s=0
        for i in n_string:
            p*=int(i)
            s+=int(i)
        return n%(p+s)==0
